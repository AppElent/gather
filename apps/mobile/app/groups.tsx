import { Simple } from '../proto/screens/Simple'

export default function Groups() {
  return (
    <Simple
      title="Groups"
      body="Create a Group, join one by invite, leave one. The slow path, off the switcher sheet."
    />
  )
}
