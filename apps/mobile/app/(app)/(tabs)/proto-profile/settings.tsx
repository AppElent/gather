// PROTOTYPE — throwaway. See the layout in this folder.
//
// This is the one that shows the seam: the real screen's own Account and Groups
// rows push the *flat* addresses, so they escape this stack and take the bar
// with them. Left alone on purpose — that is the cost being measured.
export { default } from '../../settings'
