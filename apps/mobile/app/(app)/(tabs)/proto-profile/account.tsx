// PROTOTYPE — throwaway. See the layout in this folder for why this is a
// re-export and not a copy: the same screen, reached at a second address that
// keeps the tab bar.
export { default } from '../../account'
