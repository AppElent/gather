import { Simple } from '../proto/screens/Simple'

export default function Account() {
  return (
    <Simple
      title="Account"
      body="Name, email, password — Clerk's account surface, rebuilt natively (@appelent/auth is React DOM only)."
    />
  )
}
