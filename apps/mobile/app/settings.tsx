import { Simple } from '../proto/screens/Simple'

export default function Settings() {
  return (
    <Simple
      title="Settings"
      body="Theme (system / light / dark) and language (EN / NL) would live here. Reached from You, and belonging to no Group."
    />
  )
}
