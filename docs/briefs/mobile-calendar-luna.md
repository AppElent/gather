# Implement the approved mobile calendar plan

The user has approved the design and asked for implementation by Codex
`gpt-5.6-luna` at `high` reasoning effort in this existing Orca workspace.

Work only in `C:/Users/ericj/orca/workspaces/gather/AppElent-calendar-design`
on branch `AppElent/calendar-design`. Keep this checkout; do not create another
worktree. The launching agent is handing implementation to you and will stop
editing after the launch confirmation.

Read the repository instructions, then implement the entire plan at:

`C:/Users/ericj/orca/workspaces/gather/AppElent-calendar-design/docs/plans/2026-09-12-mobile-calendar.md`

That plan is the source of truth for design, scope, milestone order, API contracts
and verification. Explicit Save with the checkmark is confirmed. Mobile first,
local calendars; web later. Complete the eight milestones and their acceptance
checks. Perform the early device gesture checkpoint before later integration.
Use applicable installed skills, including the Convex skill before backend edits
and agent-device for device verification. Progress autonomously within the plan;
report a concrete blocker when an external requirement prevents further progress.

Inspect the actual working tree first. The plan and this brief were created by
the launching session and may be untracked; preserve them and include them with
the task's documentation. Preserve any unrelated user changes. Commit task work
incrementally at coherent milestones, rather than leaving a single final commit.
Run relevant checks before each milestone commit and the full checks at completion.
Never weaken tests to pass them. Do not push, open a PR, merge, publish a release,
deploy production, or reset shared sample data; those are outside this handoff.

Keep milestone completion and evidence accurate in the plan or a linked progress
note. Finish with the resulting behavior, commit references, checks/device evidence,
and any remaining verification gaps. Never claim iOS or release-build behavior
verified from Android development-build evidence.
